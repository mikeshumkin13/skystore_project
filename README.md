echo "# 🛍️ SkyStore

Учебный Django-проект — мини-магазин плагинов и утилит.  
Домашнее задание 21.2 — шаблоны, статика, формы.

## 📦 Функционал
- Главная страница с карточкой продукта
- Страница \"Контакты\" с формой обратной связи
- Обработка POST-запроса и вывод данных в консоль
- Перенаправление на страницу благодарности
- Подключение Bootstrap через статику

## 🗂️ Структура проекта
\`\`\`
skystore_project/
├── catalog/
│   ├── static/                # Bootstrap файлы
│   ├── templates/catalog/     # HTML-шаблоны
│   ├── views.py               # Контроллеры
│   └── urls.py                # Маршруты приложения
├── skystore/
│   ├── settings.py            # Настройки Django
│   └── urls.py                # Главный роутинг
└── manage.py
\`\`\`

## 🚀 Как запустить
\`\`\`bash
pip install -r requirements.txt
python manage.py runserver
\`\`\`

## 🔧 Требования
- Python 3.12
- Django 5.2

---

## Домашнее задание 23 — Работа с ORM в Django

Реализовано:
- Модели `Category` и `Product`, связаны через ForeignKey.
- Настроена админка Django для управления товарами и категориями.
- Главная страница отображает последние добавленные товары.
- Реализована форма обратной связи на странице Контакты.
- Создана страница благодарности после отправки формы.
- Данные сериализованы в фикстуру `catalog_data.json` и загружаются кастомной командой `load_catalog_data`.


## Домашнее задание 24

В этом домашнем задании была проведена реорганизация структуры приложения `catalog` и реализованы следующие изменения:

1. **Структура `views`:**
   - Вынесены контроллеры `home`, `contacts` и `product` в отдельные файлы:
     - `views/home.py`
     - `views/contacts.py`
     - `views/product.py`
   - Создан файл `views/__init__.py` для удобного импорта контроллеров.

2. **URL конфигурация:**
   - Обновлён `urls.py` с учётом новой структуры `views`.
   - Добавлены пути для страниц:
     - Главная (`home`)
     - Добавить продукт (`add_product`)
     - Контакты (`contacts`)
     - Страница товара (`product_detail`)
     - Страница благодарности (`thank_you`)

3. **Шаблоны:**
   - Обновлены шаблоны:
     - `home.html` — добавлена кнопка **Добавить продукт** и кнопка **Подробнее**.
     - `add_product.html` — форма добавления продукта.
     - `product_detail.html` — вывод информации о товаре.
     - `contacts.html` — форма обратной связи.
     - `thank_you.html` — страница благодарности.

4. **Форма ProductForm:**
   - Форма для добавления новых продуктов реализована и интегрирована в контроллер `add_product`.


📝 Задание выполнено в рамках курса SkyPro  
Автор: [@mikeshumkin13](https://github.com/mikeshumkin13)
